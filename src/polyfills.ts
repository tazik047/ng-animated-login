/***************************************************************************************************
 * Angular requires Zone.js to support the change detection mechanism in the browser.
 * The polyfill is loaded here so it is available before the application bootstraps.
 */
import 'zone.js';

/***************************************************************************************************
 * You can add any additional polyfills that your application needs below. The Angular CLI keeps
 * this file deliberately short for evergreen browsers, so include only what you really need.
 */
